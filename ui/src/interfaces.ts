export interface DesignComponentCardProps {
    imgSrc: string;
    componentName: string;
    tags: string[];
}

export interface CanvasNodeProps {
    imgSrc: string;
    componentName: string;
    tags: string[];
    name: string;
    description?: string;
}